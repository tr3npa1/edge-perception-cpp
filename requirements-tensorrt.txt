# Optional target-machine TensorRT export environment.
#
# TensorRT Python bindings are intentionally not pinned here because their
# installation source is OS, CUDA, Python, and NVIDIA-distribution specific.
# Install the TensorRT 10.9 Python bindings supplied with the target SDK or
# container, verify `python -c "import tensorrt"`, then install this file.

-r requirements-gpu.txt
